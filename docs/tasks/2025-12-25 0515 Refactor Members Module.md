- [x] `js/members.js` থেকে `localStorage` এর সকল কোড এবং `Storage` অবজেক্টের ব্যবহার রিমুভ করা।
- [x] মেম্বার সেভ করার জন্য `/api/members` এন্ডপয়েন্টে `POST` রিকোয়েস্ট ইমপ্লিমেন্ট করা।
- [x] মেম্বার আইডি হিসেবে `Date.now().toString()` ব্যবহার নিশ্চিত করা।
- [x] মেম্বার লিস্ট লোড করার জন্য `/api/members` এন্ডপয়েন্ট থেকে `GET` রিকোয়েস্ট ইমপ্লিমেন্ট করা।
- [x] মেম্বার ডিলিট করার জন্য `/api/members/:id` এন্ডপয়েন্টে `DELETE` রিকোয়েস্ট ইমপ্লিমেন্ট করা।
- [x] সকল ফাংশন `async/await` ব্যবহার করে আপডেট করা।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
