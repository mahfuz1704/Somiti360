- [x] `js/dashboard.js` এর `updateStats` ফাংশনটি আপডেট করা এবং সরাসরি API থেকে ডাটা আনা।
- [x] `/api/members`, `/api/deposits`, `/api/loans`, `/api/expenses` এন্ডপয়েন্ট থেকে ডাটা ফেচ করা।
- [x] ফেচ করা ডাটা থেকে লুপ চালিয়ে মোট জমার পরিমাণ (Total Deposits) বের করা।
- [x] ফেচ করা ডাটা থেকে মোট ব্যয়ের পরিমাণ (Total Expenditure = Expenses + Donations) বের করা।
- [x] লোন এবং রিফান্ড এর হিসাব আপডেট করা।
- [x] ড্যাশবোর্ডের কার্ডগুলোতে রিয়েল-টাইম ভ্যালু আপডেট করা।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
