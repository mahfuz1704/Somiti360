# Task Checklist - 2025-12-24 0115 Loan and Expense Management

## লোন ম্যানেজমেন্ট (Loan Management)
- [x] `loans` টেবিল তৈরি করা (Create loans table in db.sql)
- [x] `loan_payments` টেবিল তৈরি করা (Create loan_payments table for installments)
- [x] `server.js`-এ loans এবং loan_payments API endpoint যোগ করা
- [x] `js/loans.js` মডিউল তৈরি করা (Create loans module)
- [x] `index.html`-এ লোন মেনু এবং পেজ যোগ করা (Add loan menu and page)
- [x] লোন দেওয়ার ফর্ম তৈরি করা (Create loan disbursement form)
- [x] লোন পরিশোধের ফর্ম তৈরি করা (Create loan payment form)
- [x] Dashboard-এ লোনের সারসংক্ষেপ যোগ করা (Add loan summary to dashboard)

## খরচ ম্যানেজমেন্ট (Expense Management)
- [x] `expenses` টেবিল তৈরি করা (Create expenses table in db.sql)
- [x] `server.js`-এ expenses API endpoint যোগ করা
- [x] `js/expenses.js` মডিউল তৈরি করা (Create expenses module)
- [x] `index.html`-এ খরচ মেনু এবং পেজ যোগ করা (Add expense menu and page)
- [x] খরচ যোগ করার ফর্ম তৈরি করা (Create expense add form)
- [x] Dashboard-এ খরচের সারসংক্ষেপ যোগ করা (Add expense summary to dashboard)

## Migration এবং Testing
- [x] `migration.js`-এ নতুন টেবিলের জন্য কোড যোগ করা
- [x] সব ফিচার পরীক্ষা করা (Test all features)
- [ ] Git commit করা
