- [x] `js/app.js` ফাইলে গ্লোবাল `API_URL` ভেরিয়েবল ডিফাইন করা (লোকালহোস্ট ও প্রোডাকশন ডিটেকশন সহ)।
- [x] একটি গ্লোবাল `apiCall` হেল্পার ফাংশন তৈরি করা যা সরাসরি `fetch` ব্যবহার করবে।
- [x] `js/storage.js` এর নির্ভরশীলতা কমানো এবং মডিউলগুলোতে (`Members`, `Deposits`, ইত্যাদি) এটি আপডেট করা।
- [x] `js/utils.js` তৈরি করা এবং এপিআই হেল্পার ও ইউটিলিটিগুলো সেখানে মুভ করা।
- [x] `server.js` এ রেকর্ড আপডেট (Update) করার জন্য এন্ডপয়েন্ট যুক্ত করা।
- [x] অ্যাপ লোড হওয়ার সময় সার্ভার থেকে ডাটা রিফ্রেশ (Refresh) করার মেকানিজম যুক্ত করা।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
