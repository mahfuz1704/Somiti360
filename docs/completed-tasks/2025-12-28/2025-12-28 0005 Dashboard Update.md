- [x] ড্যাশবোর্ডে "সর্বমোট সহায়তা" (Total Donation) নামে একটি নতুন স্ট্যাটাস কার্ড যুক্ত করা।
- [x] `js/dashboard.js` এ `updateStats` ফাংশন আপডেট করা যাতে donations এর হিসাব "সর্বমোট সহায়তা" কার্ডে দেখানো হয় এবং "সর্বমোট ব্যয়" থেকে বাদ দেওয়া হয়।
- [x] ড্যাশবোর্ড কার্ডগুলোর লেআউট পরিবর্তন করা (বড় স্ক্রিনে ২ সারিতে ৩টি করে কার্ড)।
- [x] পরিবর্তনগুলো যাচাই করা।
