# ড্যাশবোর্ড স্ট্যাটাস সোয়াপ (বর্তমান ব্যালেন্স ও আমানত)

## Task List

 - [x] `index.html` এ হিরো সেকশনে "সর্বমোট আমানত" এবং আইডি `totalDeposits` বসানো
 - [x] `index.html` এ স্ট্যাটাস কার্ডে "বর্তমান ব্যালেন্স" এবং আইডি `currentBalance` বসানো
 - [x] `js/dashboard.js` চেক করা (যদি কোনো লজিক্যাল পরিবর্তন লাগে)
 - [x] ব্রাউজারে পজিশন এবং ডাটা লোডিং ভেরিফাই করা
 - [x] টাস্ক লিস্ট `completed-tasks` ফোল্ডারে সরানো
