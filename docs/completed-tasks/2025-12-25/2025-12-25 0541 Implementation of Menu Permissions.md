- [x] ডাটাবেসের `users` টেবিলে `permissions` কলাম যোগ করার SQL তৈরি ও রান করা।
- [x] `server.js` আপডেট করা যাতে ইউজারের পারমিশন ডাটা সঠিকভাবে হ্যান্ডেল হয়।
- [x] `js/auth.js` এর `hasPermission` এবং `updateSidebar` লজিক সুপার অ্যাডমিন ও পারমিশন লিস্ট অনুযায়ী আপডেট করা।
- [x] `js/users.js` অ্যাডমিন প্যানেলে ইউজার তৈরি/সম্পাদনার সময় পারমিশন চেকবক্স যোগ করা।
- [x] পারমিশন ডাটা সেভ করার মেকানিজম আপডেট করা।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
