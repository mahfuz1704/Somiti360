- [x] `server.js` ফাইলে `allowedTables` লিষ্টটি এক জায়গায় নিয়ে আসা।
- [x] `GET`, `POST`, এবং `DELETE` সকল এপিআই (API) রুটে টেবিল ভ্যালিডেশন যুক্ত করা।
- [x] `POST` এবং `DELETE` রুটে ডাটা ভ্যালিডেশন এবং এরর হ্যান্ডলিং ইমপ্রুভ করা।
- [/] পরিবর্তনগুলো commit এবং push করা।
