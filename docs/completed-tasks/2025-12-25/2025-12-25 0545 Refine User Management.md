- [x] `js/app.js` এর ইউজার রোল ড্রপডাউনে `superadmin`, `admin`, এবং `user` অপশনগুলো সেট করা।
- [x] রোল সিলেকশন পরিবর্তনের সময় `superadmin` হলে সকল পারমিশন স্বয়ংক্রিয়ভাবে সিলেক্ট করার লজিক যোগ করা।
- [x] `js/users.js` এ ডিফল্ট রোল এবং পারমিশন হ্যান্ডলিং চেক করা।
- [x] `server.js` এ ডাটা সেভিং মেকানিজম ভেরিফাই করা।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
