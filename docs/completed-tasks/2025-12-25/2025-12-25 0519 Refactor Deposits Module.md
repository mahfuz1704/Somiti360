- [x] `js/deposits.js` থেকে `localStorage` এর সকল কোড এবং `Storage` অবজেক্টের ব্যবহার রিমুভ করা।
- [x] জমা আইডি হিসেবে `Date.now().toString()` ব্যবহার নিশ্চিত করা।
- [x] নতুন জমা সেভ করার জন্য `/api/deposits` এন্ডপয়েন্টে `POST` রিকোয়েস্ট ইমপ্লিমেন্ট করা।
- [x] সফলভাবে জমা সেভ হওয়ার পর `Dashboard.refresh()` কল করে ড্যাশবোর্ড আপডেট করা।
- [x] জমা ডিলিট করার পর `Dashboard.refresh()` কল করে ড্যাশবোর্ড আপডেট করা।
- [x] সকল ফাংশন `async/await` ব্যবহার করে আপডেট করা।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
