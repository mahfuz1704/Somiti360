- [x] `js/auth.js` এর `login` ফাংশনটি আপডেট করা এবং সরাসরি API থেকে ইউজার ডাটা আনা।
- [x] `/api/users` থেকে ইউজারদের তালিকা এনে ইউজারনেম এবং পাসওয়ার্ড চেক করা।
- [x] সফলভাবে লগইন হলে ইউজারের তথ্য `sessionStorage` এ সংরক্ষণ করা।
- [x] `getCurrentUser`, `isAuthenticated`, `logout` ফাংশনগুলো `sessionStorage` অনুযায়ী আপডেট করা।
- [x] সুপার অ্যাডমিনের জন্য ডিক্রিপশন বা ফিক্সড লজিক চেক করা (প্রয়োজন হলে)।
- [x] পরিবর্তনগুলো ভেরিফাই (Verify) করা।
